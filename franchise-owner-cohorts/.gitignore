# People data NEVER goes to git — this repository is public.
# Data home is Google Drive; raw/processed/staging/outputs live there.
data/
outputs/

# Cohort Lab deploy data: only the encrypted .enc files are committed.
cohort_lab/.data_key
cohort_lab/deploy/*.parquet

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
